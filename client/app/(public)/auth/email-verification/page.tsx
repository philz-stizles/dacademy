import { EmailVerificationForm } from '@/components/auth/email-verification-form/email-verification-form';

const EmailVerificationPage = () => {
  return <EmailVerificationForm />;
};

export default EmailVerificationPage;
