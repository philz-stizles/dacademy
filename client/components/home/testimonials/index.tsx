import { Container } from '@/components/shared';

type Props = {};

const Testimonials = (props: Props) => {
  return (
    <section>
      <Container></Container>
    </section>
  );
};

export default Testimonials;
